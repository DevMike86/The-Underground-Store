<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Underground Store</title>
    <style>
        body { background-color: #0d1117; color: #c9d1d9; font-family: 'Segoe UI', sans-serif; text-align: center; padding: 50px; }
        .card { background: #161b22; border: 1px solid #30363d; border-radius: 12px; padding: 30px; display: inline-block; max-width: 400px; box-shadow: 0 8px 24px rgba(0,0,0,0.5); }
        h1 { color: #58a6ff; margin-bottom: 10px; }
        .status { font-weight: bold; padding: 10px; border-radius: 6px; background: #21262d; display: block; margin: 20px 0; }
        .btn { background-color: #238636; color: white; padding: 15px 30px; text-decoration: none; border-radius: 6px; font-weight: bold; display: inline-block; transition: 0.3s; }
        .btn:hover { background-color: #2ea043; }
        #api-status { color: #ff7b72; } /* Default Offline Red */
    </style>
</head>
<body>

    <div class="card">
        <h1>The Underground Store</h1>
        <p>Uncensored AI. Total Privacy. 2026.</p>
        
        <div class="status">
            SYSTEM STATUS: <span id="api-status">OFFLINE</span>
        </div>

        <p style="font-size: 0.8rem; opacity: 0.6; margin-bottom: 25px;">
            Connection: Your Private Home API
        </p>

        <a href="YOUR_APK_NAME_HERE.apk" class="btn">DOWNLOAD AI APP</a>
    </div>

    <script>
        // PASTE YOUR NGROK URL HERE (Keep the /v1)
        const myAPI = "https://  <script>
        // Use your real ngrok link here!
        const myAPI = "https://  https://unadept-elocutionary-ling.ngrok-free.dev -> http://localhost:1234/v1"; 

        async function checkStatus() {
            try {
                // We add /models to the end to see if the AI engine responds
                const response = await fetch(myAPI + "/models", {
                    headers: { "ngrok-skip-browser-warning": "true" }
                });
                if (response.ok) {
                    const statusEl = document.getElementById('api-status');
                    if(statusEl) {
                        statusEl.innerText = "ONLINE (UNCENSORED)";
                        statusEl.style.color = "#00ffcc";
                    }
                }
            } catch (e) { 
                console.log("Checking for AI engine..."); 
            }
        }
        // Run the check as soon as the page opens
        checkStatus();
    </script>
</body>
</html>
/v1"; 

        async function checkStatus() {
            try {
                const response = await fetch(myAPI + "/models", {
                    headers: { "ngrok-skip-browser-warning": "true" }
                });
                if (response.ok) {
                    const statusEl = document.getElementById('api-status');
                    statusEl.innerText = "ONLINE (UNCENSORED)";
                    statusEl.style.color = "#3fb950"; // Switch to Green
                }
            } catch (e) { console.log("AI is sleeping..."); }
        }
        checkStatus();
    </script>
</body>
</html>
